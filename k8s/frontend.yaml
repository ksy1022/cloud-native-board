apiVersion: v1
kind: Service
metadata:
  name: frontend-svc
  namespace: board-app
spec:
  type: NodePort
  selector:
    app: frontend
  ports:
    - port: 80         # 클러스터 내부에서 사용하는 포트
      targetPort: 80   # 컨테이너 포트
      nodePort: 30080  # 🔥 외부에서 접속할 NodePort (나중에 포트포워딩에 쓰일 애)
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: frontend-deploy
  namespace: board-app
spec:
  replicas: 1
  selector:
    matchLabels:
      app: frontend
  template:
    metadata:
      labels:
        app: frontend
    spec:
      containers:
        - name: frontend
          # 🔥 이것도 나중에 Docker Hub 이미지 이름이랑 맞출 거야
          image: yeob1022/board-frontend:latest
          imagePullPolicy: Always
          ports:
            - containerPort: 80
